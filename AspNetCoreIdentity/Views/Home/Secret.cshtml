@using AspNetCoreIdentity.Extensions
@{
    ViewData["Title"] = "Shhhhhhhhhhhhh!";
}
<h1>@ViewData["Title"]</h1>

<p>This is a secret page!</p>

@{ 
    if (this.IfClaim("Produtos", "Adicionar")) 
    {
        <p>Você só verá isso se tiver permissão de adicionar</p>
    }
}

<div>
    @Html.ActionLink("Secret", "Secret").IfClaimShow(Context, "Produtos", "Navegar")
</div>
<br/>
<div>
    <a href="#" class="btn btn-danger @Html.Raw(this.IfClaimShow("Produtos", "Navegar"))">Excluir</a>
</div>